/*
 * File: 2-strlen.c
 * Auth: Amin Israfilov
 */

#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns the length of a string.
 * @s: The string to get the length of.
 *
 * Return: The length of @str.
 */
void _puts(char *str)
{
    puts(str);
}
