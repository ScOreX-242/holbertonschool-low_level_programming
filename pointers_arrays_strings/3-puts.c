/*
 * File: 2-strlen.c
 * Auth: Amin Israfilov
 */

#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @s: The string to get the length of.
 *
 * Return: The length of @str.
 */
void _puts(char *str)
{
    puts(str);
    return 0;
}
